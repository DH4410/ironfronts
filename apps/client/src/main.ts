// Stable browser entry. The renderer/UI implementation remains in the shared
// source tree while the monorepo migration settles, but all application boot
// now begins from the client workspace.
import '../../../src/main';
